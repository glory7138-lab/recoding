# NativeBOX AI - 동영상 문장 자동 분할 어학 플레이어

## 📌 배포 방식 안내

---

## 방식 1: `.exe` 파일로 배포 (가장 쉬운 방법)

**상대방이 할 일: 파일 더블클릭만 하면 끝!**

### 개발자(나)가 만드는 법:
터미널에서 아래 명령어 2개 실행:

```bash
npm run build   # 앱 빌드 (30초~1분)
npm run dist    # exe 파일 생성 (2~3분)
```

완료 후 `dist/NativeBOX_AI_Player.exe` 파일이 생성됩니다.

이 파일을 상대방에게 전달하면 끝!

> ⚠️ 참고: 파일 크기가 약 100~200MB 정도입니다. Google Drive, 네이버 클라우드, USB 등으로 전달하세요.

---

## 방식 2: 소스코드 ZIP + 자동 실행 배치파일 (Node.js 필요)

**상대방이 할 일:**
1. [Node.js 20 LTS](https://nodejs.org/ko) 설치 (1회)
2. 받은 ZIP 파일 압축 해제
3. `실행하기.bat` 더블클릭 → 자동 설치 후 앱 실행

### 개발자(나)가 배포할 파일:
아래 파일/폴더를 ZIP으로 묶어서 전달:

```
📁 recoding/ (폴더 이름 변경 가능)
├── app/
├── public/
├── docs/
├── package.json
├── next.config.mjs
├── main.js
├── 실행하기.bat    ← 이 파일이 핵심!
└── (node_modules/ 제외, out/ 제외)
```

> ✅ `실행하기.bat`이 자동으로: 패키지 설치 → 빌드 → 앱 실행을 순서대로 처리합니다.

---

## 방식 비교

| | exe 방식 | ZIP + bat 방식 |
|---|---|---|
| 상대방 요구 조건 | 없음 (더블클릭만) | Node.js 설치 필요 |
| 파일 크기 | ~150MB | ~5MB (node_modules 제외) |
| 최초 실행 시간 | 즉시 | 자동 설치 포함 3~5분 |
| AI 기능 | 동일 | 동일 |
| 권장 대상 | 비개발자 일반 사용자 | 개발자 또는 Node.js 이미 설치된 사람 |
